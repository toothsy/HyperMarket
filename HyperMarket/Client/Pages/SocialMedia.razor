@page "/SocialMedia"

@using HyperMarket.DB.Models;
@using System.Text.RegularExpressions
@using System.ComponentModel.DataAnnotations
@inject HyperMarket.Data.Interfaces.IConfigService configService
<div class="bg">
    <MudGrid>
        <div class="first">
            <MudItem xs="12" sm="7">
                <div class="second">
                    <MudPaper Class="mx-auto pa-6">
                        <h3>Follow Us On</h3>
                        <MudIconButton Icon="@Icons.Custom.Brands.Facebook" Color="Color.Primary" aria-label="facebook"></MudIconButton>
                        <p>@config.FaceBookHandle</p>
                        <MudIconButton Icon="@Icons.Custom.Brands.Instagram" Color="Color.Primary" aria-label="instagram"></MudIconButton>
                        <p>@config.InstaHandle</p>
                        <MudIconButton Icon="@Icons.Custom.Brands.Twitter" Color="Color.Primary" aria-label="teitter"></MudIconButton>
                        <p>@config.TwitterHandle</p>

                    </MudPaper>
                </div>
            </MudItem>
        </div>
    </MudGrid>
</div>

@code {
    public Config config = new Config();

    protected override async Task OnInitializedAsync()
    {
        config = await configService.GetConfigById(601);
    }
}